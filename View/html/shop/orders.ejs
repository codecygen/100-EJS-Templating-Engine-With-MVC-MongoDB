<%- include('../includes/head.ejs') %>
</head>
<body>
    <%- include('../includes/nav.ejs') %>

    <main>
        <% if (orderList.length > 0) { %>
            <% let i = 1 %>
            <% orderList.forEach((order) => { %>
                <p>Order <%= i %></p>
                <% order.forEach((product) => { %>
                    <ul>
                        <li><%= product.productName %> (<%= product.qty %>)</li>
                    </ul>
                <% }); %>

                <% i++ %>
            <% }); %>
        <% } else { %>
            <p>Nothing to show in orders page yet!</p>
        <% } %>
    </main>

</body>
</html>